/*dotenv is a package that store private env varibles into public accessible, encrypted
process.env variable to be accessed without exposing data*/

import dotenv from 'dotenv'
dotenv.config({path:'../.env'})
